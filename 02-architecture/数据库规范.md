# XTrendAI 数据库设计规范 v2.1

## 1. 存储方案

### 1.1 MVP阶段 (V1.0)

| 数据类型 | 存储方式 | 文件路径 |
|---------|---------|---------|
| 用户数据 | JSON文件 | `data/users.json` |
| 热点数据 | JSON文件 | `data/mock-trends.json` |
| 任务模板 | JSON文件 | `data/templates.json` |
| 翻译资源 | JSON文件 | `locales/zh-CN.json`, `locales/en-US.json` |
| 配额数据 | 内存缓存 | - |
| 用户配置 | LocalStorage | 浏览器 |

### 1.2 未来演进 (V2.0)

| 数据类型 | 存储方式 | 说明 |
|---------|---------|------|
| 用户数据 | SQLite/PostgreSQL | 持久化存储 |
| 热点缓存 | Redis | 高速缓存 |
| 任务模板 | 数据库 | 可动态管理 |
| 翻译资源 | 数据库/COS | 支持动态管理 |
| 操作日志 | 数据库 | 审计追踪 |

---

## 2. JSON数据结构

### 2.1 用户数据 (data/users.json)

```json
{
  "users": [
    {
      "id": "user-guest-001",
      "email": null,
      "name": "Guest User",
      "role": "guest",
      "language": "zh-CN",
      "profile": {
        "region": "global",
        "ageGroup": "25-34",
        "ethnicity": null,
        "scenarios": ["POD"]
      },
      "quota": {
        "dailyFetch": -1,
        "dailyCopy": 3,
        "scenarioCount": 1,
        "refreshInterval": 0
      },
      "createdAt": "2026-02-26T10:00:00Z",
      "lastLoginAt": "2026-02-26T10:00:00Z"
    },
    {
      "id": "user-free-001",
      "email": "user@example.com",
      "name": "John Doe",
      "role": "free",
      "language": "en-US",
      "profile": {
        "region": "us",
        "ageGroup": "25-34",
        "ethnicity": "asian",
        "scenarios": ["POD", "MARKETING"]
      },
      "quota": {
        "dailyFetch": 10,
        "dailyCopy": 20,
        "scenarioCount": 2,
        "refreshInterval": 60
      },
      "usage": {
        "date": "2026-02-26",
        "fetchCount": 3,
        "copyCount": 5
      },
      "createdAt": "2026-02-20T10:00:00Z",
      "updatedAt": "2026-02-26T09:00:00Z",
      "lastLoginAt": "2026-02-26T08:00:00Z"
    }
  ]
}
```

### 2.2 热点数据 (data/mock-trends.json)

```json
{
  "updatedAt": "2026-02-26T10:00:00Z",
  "trends": [
    {
      "id": "trend-superbowl-2026",
      "name": "#SuperBowl2026",
      "displayName": "SuperBowl2026",
      "volume": 1200000,
      "growthRate": 15.2,
      "category": "sports",
      "regions": ["us"],
      "isPublic": true,
      "relatedTweets": 1200000,
      "url": "https://x.com/search?q=%23SuperBowl2026",
      "keywords": ["football", "nfl", "super bowl", "championship"],
      "demographics": {
        "ageGroups": ["18-24", "25-34", "35-44"],
        "regions": ["us"]
      }
    },
    {
      "id": "trend-oscars-2026",
      "name": "#Oscars2026",
      "displayName": "Oscars2026",
      "volume": 890000,
      "growthRate": 8.5,
      "category": "entertainment",
      "regions": ["global"],
      "isPublic": true,
      "relatedTweets": 890000,
      "url": "https://x.com/search?q=%23Oscars2026",
      "keywords": ["oscars", "academy awards", "movies", "film"],
      "demographics": {
        "ageGroups": ["18-24", "25-34", "35-44", "45+"],
        "regions": ["global"]
      }
    },
    {
      "id": "trend-ai-news-2026",
      "name": "#AINews",
      "displayName": "AINews",
      "volume": 450000,
      "growthRate": 22.5,
      "category": "tech",
      "regions": ["us", "cn", "global"],
      "isPublic": false,
      "relatedTweets": 450000,
      "url": "https://x.com/search?q=%23AINews",
      "keywords": ["ai", "artificial intelligence", "tech", "machine learning"],
      "demographics": {
        "ageGroups": ["25-34", "35-44"],
        "regions": ["us", "cn"]
      }
    }
  ]
}
```

### 2.3 任务模板 (data/templates.json)

```json
{
  "version": "2.0",
  "updatedAt": "2026-02-26T10:00:00Z",
  "scenarios": {
    "POD": {
      "id": "scenario-pod",
      "name": "POD按需打印",
      "description": "生成POD商品的设计提示词",
      "taskTypes": [
        {
          "id": "pod-tshirt",
          "name": "T恤设计",
          "template": "A trendy t-shirt design for {trendName}, {styleHint}. {additionalSpecs}",
          "styleHints": [
            "featuring a stylized graphic with team colors",
            "with bold typography and modern design",
            "minimalist vector art style",
            "vintage retro aesthetic with distressed effects"
          ],
          "additionalSpecs": "suitable for print-on-demand, high contrast, clean lines",
          "defaultContent": "A trendy t-shirt design for {trendName}, featuring a stylized graphic with modern colors, minimalist vector art style, suitable for print-on-demand"
        },
        {
          "id": "pod-tote",
          "name": "帆布袋设计",
          "template": "A chic tote bag design inspired by {trendName}, {styleHint}. {additionalSpecs}",
          "styleHints": [
            "pastel color palette",
            "earthy tones with nature elements",
            "bold geometric patterns",
            "aesthetic illustration style"
          ],
          "additionalSpecs": "aesthetic style, suitable for bag printing, medium complexity",
          "defaultContent": "A chic tote bag design inspired by {trendName}, pastel color palette, aesthetic illustration style"
        },
        {
          "id": "pod-mug",
          "name": "马克杯设计",
          "template": "A coffee mug design themed around {trendName}, {styleHint}. {additionalSpecs}",
          "styleHints": [
            "fun and quirky",
            "elegant and minimal",
            "colorful and vibrant",
            "typography focused"
          ],
          "additionalSpecs": "wraparound design or centered, suitable for mug printing",
          "defaultContent": "A coffee mug design themed around {trendName}, fun and quirky style, vibrant colors"
        },
        {
          "id": "pod-phonecase",
          "name": "手机壳设计",
          "template": "A phone case design for {trendName} fans, {styleHint}. {additionalSpecs}",
          "styleHints": [
            "sporty typography",
            "cute kawaii style",
            "geometric patterns",
            "gradient color scheme"
          ],
          "additionalSpecs": "modern graphic design, suitable for phone case printing",
          "defaultContent": "A phone case design for {trendName} fans, sporty typography with bold colors, modern graphic design"
        }
      ]
    },
    "MARKETING": {
      "id": "scenario-marketing",
      "name": "营销文案",
      "description": "生成营销推广文案",
      "taskTypes": [
        {
          "id": "marketing-title",
          "name": "促销标题",
          "template": "{emoji} {headline}！{cta}",
          "variants": [
            "抓住 {trendName} 潮流，限时优惠！",
            "{trendName} 主题活动，不容错过！",
            "热点来袭：{trendName} 专题上线！"
          ],
          "defaultContent": "🔥 抓住 {trendName} 潮流，限时优惠！"
        },
        {
          "id": "marketing-description",
          "name": "商品描述",
          "template": "{trendName} 主题商品，{features}。{appeal}",
          "features": "精选材质，舒适耐用",
          "appeal": "限量发售，售完即止",
          "defaultContent": "{trendName} 主题商品，精选材质，舒适耐用。限量发售，售完即止"
        },
        {
          "id": "marketing-tags",
          "name": "推荐标签",
          "template": "{trendName},{relatedTags}",
          "relatedTags": "热门,潮流,新品",
          "defaultContent": "{trendName},热门,潮流,新品"
        }
      ]
    },
    "CONTENT": {
      "id": "scenario-content",
      "name": "内容创作",
      "description": "生成内容创作方向和脚本大纲",
      "taskTypes": [
        {
          "id": "content-video-topic",
          "name": "视频选题",
          "template": "{title}\n\n大纲：\n{outline}",
          "title": "{trendName} 热点解读",
          "outline": [
            "1. 开场：{trendName} 为什么火？",
            "2. 背景介绍：事件来龙去脉",
            "3. 深度分析：背后的原因",
            "4. 趋势预测：接下来会怎样？",
            "5. 互动：你如何看待 {trendName}？"
          ],
          "defaultContent": "{trendName} 热点解读\n\n大纲：\n1. 开场：{trendName} 为什么火？\n2. 背景介绍：事件来龙去脉\n3. 深度分析：背后的原因\n4. 趋势预测：接下来会怎样？\n5. 互动：你如何看待 {trendName}？"
        },
        {
          "id": "content-script-outline",
          "name": "脚本大纲",
          "template": "【{trendName} 主题视频脚本】\n\n时长：{duration}\n风格：{style}\n\n{sections}",
          "duration": "3-5分钟",
          "style": "轻松幽默",
          "sections": "开场(30秒) → 主体(2-3分钟) → 结尾(30秒)",
          "defaultContent": "【{trendName} 主题视频脚本】\n\n时长：3-5分钟\n风格：轻松幽默\n\n开场(30秒) → 主体(2-3分钟) → 结尾(30秒)"
        }
      ]
    },
    "DEVELOPMENT": {
      "id": "scenario-development",
      "name": "快速开发",
      "description": "生成开发需求描述和技术方向",
      "taskTypes": [
        {
          "id": "dev-requirement",
          "name": "功能需求",
          "template": "【{trendName} 相关工具需求】\n\n目标：{goal}\n\n核心功能：\n{features}",
          "goal": "帮助用户快速获取 {trendName} 相关信息",
          "features": [
            "- 实时数据展示",
            "- 个性化推荐",
            "- 数据可视化"
          ],
          "defaultContent": "【{trendName} 相关工具需求】\n\n目标：帮助用户快速获取 {trendName} 相关信息\n\n核心功能：\n- 实时数据展示\n- 个性化推荐\n- 数据可视化"
        },
        {
          "id": "dev-tech-stack",
          "name": "技术方向",
          "template": "推荐技术栈：\n\n前端：{frontend}\n后端：{backend}\n数据库：{database}\n部署：{deployment}",
          "frontend": "Next.js + Tailwind CSS",
          "backend": "Node.js + Express",
          "database": "PostgreSQL",
          "deployment": "Vercel + Railway",
          "defaultContent": "推荐技术栈：\n\n前端：Next.js + Tailwind CSS\n后端：Node.js + Express\n数据库：PostgreSQL\n部署：Vercel + Railway"
        }
      ]
    }
  }
}
```

### 2.4 地区配置 (data/regions.json)

```json
{
  "regions": [
    {
      "id": "us",
      "name": "美国",
      "flag": "🇺🇸",
      "language": "en",
      "currency": "USD",
      "timezone": "America/New_York"
    },
    {
      "id": "cn",
      "name": "中国",
      "flag": "🇨🇳",
      "language": "zh",
      "currency": "CNY",
      "timezone": "Asia/Shanghai"
    },
    {
      "id": "uk",
      "name": "英国",
      "flag": "🇬🇧",
      "language": "en",
      "currency": "GBP",
      "timezone": "Europe/London"
    },
    {
      "id": "jp",
      "name": "日本",
      "flag": "🇯🇵",
      "language": "ja",
      "currency": "JPY",
      "timezone": "Asia/Tokyo"
    },
    {
      "id": "global",
      "name": "全球",
      "flag": "🌏",
      "language": "en",
      "currency": "USD",
      "timezone": "UTC"
    }
  ]
}
```

### 2.5 场景配置 (data/scenarios.json)

```json
{
  "scenarios": [
    {
      "id": "POD",
      "name": "POD按需打印",
      "description": "生成POD商品的设计提示词",
      "icon": "👕",
      "availableFor": ["guest", "free", "pro"],
      "maxTasks": 4
    },
    {
      "id": "MARKETING",
      "name": "营销文案",
      "description": "生成营销推广文案",
      "icon": "📢",
      "availableFor": ["free", "pro"],
      "maxTasks": 3
    },
    {
      "id": "CONTENT",
      "name": "内容创作",
      "description": "生成内容创作方向",
      "icon": "✍️",
      "availableFor": ["free", "pro"],
      "maxTasks": 2
    },
    {
      "id": "DEVELOPMENT",
      "name": "快速开发",
      "description": "生成开发需求描述",
      "icon": "💻",
      "availableFor": ["pro"],
      "maxTasks": 2
    }
  ]
}
```

---

## 3. 数据操作规范

### 3.1 用户数据操作

```typescript
// stores/UserStore.ts

interface UserStore {
  // 获取用户
  getUser(userId: string): Promise<User | null>

  // 创建用户
  createUser(userData: Omit<User, 'id' | 'createdAt'>): Promise<User>

  // 更新用户
  updateUser(userId: string, updates: Partial<User>): Promise<User>

  // 更新配额使用
  updateUsage(userId: string, date: string, usage: UsageUpdate): Promise<void>
}

interface UsageUpdate {
  fetchCount?: number
  copyCount?: number
}
```

### 3.2 热点数据操作

```typescript
// stores/TrendStore.ts

interface TrendStore {
  // 获取所有热点
  getAllTrends(): Promise<Trend[]>

  // 根据ID获取热点
  getTrendById(trendId: string): Promise<Trend | null>

  // 根据地区筛选
  getTrendsByRegion(region: string): Promise<Trend[]>

  // 获取公共热点
  getPublicTrends(): Promise<Trend[]>

  // 刷新热点数据
  refreshTrends(): Promise<Trend[]>
}
```

### 3.3 任务模板操作

```typescript
// stores/TemplateStore.ts

interface TemplateStore {
  // 获取所有场景
  getAllScenarios(): Promise<Scenario[]>

  // 获取场景详情
  getScenario(scenarioId: string): Promise<Scenario | null>

  // 获取任务类型
  getTaskType(scenarioId: string, taskTypeId: string): Promise<TaskType | null>

  // 渲染模板
  renderTemplate(template: string, variables: Record<string, string>): string
}
```

---

## 4. 未来数据库设计 (V2.0)

### 4.1 PostgreSQL表结构

#### users 表

```sql
CREATE TABLE users (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  email VARCHAR(255) UNIQUE NOT NULL,
  name VARCHAR(255),
  password_hash VARCHAR(255),
  role VARCHAR(20) NOT NULL DEFAULT 'free',
  language VARCHAR(10) NOT NULL DEFAULT 'zh-CN',
  created_at TIMESTAMP DEFAULT NOW(),
  updated_at TIMESTAMP DEFAULT NOW(),
  last_login_at TIMESTAMP
);

CREATE INDEX idx_users_email ON users(email);
CREATE INDEX idx_users_role ON users(role);
CREATE INDEX idx_users_language ON users(language);
```

#### user_profiles 表

```sql
CREATE TABLE user_profiles (
  user_id UUID PRIMARY KEY REFERENCES users(id) ON DELETE CASCADE,
  region VARCHAR(10) NOT NULL DEFAULT 'global',
  age_group VARCHAR(10) NOT NULL,
  ethnicity VARCHAR(20),
  scenarios JSONB NOT NULL DEFAULT '[]',
  created_at TIMESTAMP DEFAULT NOW(),
  updated_at TIMESTAMP DEFAULT NOW()
);
```

#### user_quotas 表

```sql
CREATE TABLE user_quotas (
  user_id UUID PRIMARY KEY REFERENCES users(id) ON DELETE CASCADE,
  role VARCHAR(20) NOT NULL,
  daily_fetch INTEGER NOT NULL,
  daily_copy INTEGER NOT NULL,
  scenario_count INTEGER NOT NULL,
  refresh_interval INTEGER NOT NULL
);
```

#### user_usage 表

```sql
CREATE TABLE user_usage (
  id BIGSERIAL PRIMARY KEY,
  user_id UUID NOT NULL REFERENCES users(id) ON DELETE CASCADE,
  date DATE NOT NULL,
  fetch_count INTEGER NOT NULL DEFAULT 0,
  copy_count INTEGER NOT NULL DEFAULT 0,
  created_at TIMESTAMP DEFAULT NOW(),
  updated_at TIMESTAMP DEFAULT NOW(),
  UNIQUE(user_id, date)
);

CREATE INDEX idx_user_usage_user_date ON user_usage(user_id, date);
```

#### trends 表

```sql
CREATE TABLE trends (
  id VARCHAR(100) PRIMARY KEY,
  name VARCHAR(255) NOT NULL,
  display_name VARCHAR(255),
  volume BIGINT NOT NULL DEFAULT 0,
  growth_rate DECIMAL(5,2) NOT NULL DEFAULT 0,
  category VARCHAR(50),
  regions JSONB NOT NULL DEFAULT '[]',
  is_public BOOLEAN NOT NULL DEFAULT false,
  url TEXT,
  keywords JSONB NOT NULL DEFAULT '[]',
  demographics JSONB,
  fetched_at TIMESTAMP DEFAULT NOW(),
  updated_at TIMESTAMP DEFAULT NOW()
);

CREATE INDEX idx_trends_category ON trends(category);
CREATE INDEX idx_trends_public ON trends(is_public);
CREATE INDEX idx_trends_updated ON trends(updated_at);
```

---

## 5. 数据验证规则

### 5.1 用户数据验证

```typescript
import { z } from 'zod'

export const UserProfileSchema = z.object({
  region: z.enum(['us', 'cn', 'uk', 'jp', 'global']),
  ageGroup: z.enum(['18-24', '25-34', '35-44', '45+']),
  ethnicity: z.enum(['asian', 'black', 'hispanic', 'white', 'other']).optional(),
  scenarios: z.array(z.enum(['POD', 'CONTENT', 'MARKETING', 'DEVELOPMENT']))
    .min(1)
    .max(4)
})

export const UserSchema = z.object({
  id: z.string().uuid(),
  email: z.string().email().nullable(),
  name: z.string().min(1).max(255),
  role: z.enum(['guest', 'free', 'pro', 'admin']),
  language: z.enum(['zh-CN', 'en-US']).default('zh-CN'),
  profile: UserProfileSchema,
  createdAt: z.string().datetime(),
  updatedAt: z.string().datetime()
})
```

### 5.2 热点数据验证

```typescript
export const TrendSchema = z.object({
  id: z.string().min(1),
  name: z.string().min(1).max(255),
  displayName: z.string().max(255).optional(),
  volume: z.number().int().nonnegative(),
  growthRate: z.number(),
  category: z.enum(['sports', 'tech', 'entertainment', 'politics', 'news', 'other']),
  regions: z.array(z.string()),
  isPublic: z.boolean(),
  url: z.string().url().optional(),
  updatedAt: z.string().datetime()
})
```

---

## 6. 数据安全规范

### 6.1 敏感数据处理

| 数据类型 | 处理方式 |
|---------|---------|
| 用户密码 | bcrypt哈希 |
| API密钥 | 环境变量，不提交代码 |
| 用户邮箱 | 加密存储，脱敏展示 |
| 操作日志 | 记录关键操作，不记录敏感信息 |

### 6.2 数据备份

- 开发环境：本地JSON文件定期备份
- 生产环境：数据库自动备份

---

## 7. 国际化数据结构

### 7.1 翻译资源文件

```typescript
// locales/zh-CN.json
interface ZhCNTranslations {
  common: {
    appName: string
    loading: string
    error: string
    copy: string
    copied: string
  }
  home: {
    title: string
    fetchTrends: string
    lastUpdate: string
  }
  profile: {
    title: string
    steps: {
      language: string
      region: string
      age: string
      ethnicity: string
      scenarios: string
    }
  }
  templates: {
    [scenario: string]: {
      [taskType: string]: string  // 支持参数插值
    }
  }
}

// locales/en-US.json - 结构相同
```

### 7.2 语言配置

```typescript
// config/i18n.ts
export interface LocaleConfig {
  defaultLocale: 'zh-CN' | 'en-US'
  locales: readonly ['zh-CN', 'en-US']
  localeNames: Record<string, string>
  localeFlags: Record<string, string>
}
```

---

*文档版本: v2.1*
*创建日期: 2026-02-26*
*最后更新: 2026-02-26 (新增国际化数据结构)*
*负责人: 系统架构师*
